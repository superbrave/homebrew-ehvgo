# ehvgo

Initialize the project by running `go mod init main.go` and load the modules with `go mod tidy`.

If you're planning to add a command, it's best that you install Cobra CLI:

```
go install github.com/spf13/cobra-cli@latest
```