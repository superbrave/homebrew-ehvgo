name: Build release binaries

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23.x"

      - name: Build binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          mkdir -p dist
          go build -trimpath -ldflags "-s -w" -o "dist/ehvgo-${{ matrix.goos }}-${{ matrix.goarch }}" .

      - name: Create checksum
        run: |
          sha256sum "dist/ehvgo-${{ matrix.goos }}-${{ matrix.goarch }}" | awk '{print $1}' > "dist/ehvgo-${{ matrix.goos }}-${{ matrix.goarch }}.sha256"

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: dist/ehvgo-${{ matrix.goos }}-${{ matrix.goarch }}*
